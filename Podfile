# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RESTApi' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RESTApi
  pod "SwiftyVK"
  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'SwiftyJSON'

  target 'RESTApiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RESTApiUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
